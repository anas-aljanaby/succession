services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # Standalone — served by raw IP on host port 8080, separate from the ssgm stack.
    ports:
      - "8080:80"
    restart: unless-stopped
